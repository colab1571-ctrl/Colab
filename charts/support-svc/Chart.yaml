apiVersion: v2
name: support-svc
description: Colab Support Service — FAQ retrieval, AI chatbot, ticket management, SLA timers, CSAT
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: svc
    version: "0.1.0"
    repository: "file://../../charts/svc"
    alias: svc
