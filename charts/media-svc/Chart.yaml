apiVersion: v2
name: media-svc
description: Colab Media Service — presigned S3 upload, scan pipeline, CloudFront signed URL serving
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: svc
    version: "0.1.0"
    repository: "file://../../charts/svc"
    alias: svc
