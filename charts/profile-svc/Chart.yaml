apiVersion: v2
name: profile-svc
description: Colab Profile Service — profile CRUD, portfolio, AI review, badge state machine.
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: svc
    version: ">=0.1.0"
    repository: "file://../svc"
