apiVersion: v2
name: invite-svc
description: Colab invite-svc — Vibe Check invites, rolling quota, blocks, match emission
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: svc
    version: ">=0.1.0"
    repository: "file://../svc"
