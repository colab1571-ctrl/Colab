apiVersion: v2
name: admin-svc
description: Colab Admin Service — moderator console, support console, billing admin, audit log, RBAC
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: svc
    version: "0.1.0"
    repository: "file://../../charts/svc"
    alias: svc
